import PageHeader from '../components/PageHeader'

export default function Gallery() {
  return (
    <>
      <PageHeader title="Gallery" />
      <div className="section section-shell">{/* TODO: build out a curated, captioned gallery (not an image dump like the original) */}</div>
    </>
  )
}
